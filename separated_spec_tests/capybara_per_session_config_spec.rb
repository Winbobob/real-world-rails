# frozen_string_literal: true

require 'spec_helper'
require 'capybara/dsl'

RSpec.describe Capybara::SessionConfig do
  describe "threadsafe" do
    it "defaults to global session options" 


    it "doesn't change global session when changed" 


    it "doesn't allow session configuration block when false" 


    it "doesn't allow session config when false" 


    it "uses the config from the session" 


    it "won't change threadsafe once a session is created" 

  end
end

