describe ValueHelper do
  describe "#property_display_value" do
    context "value is present" do
      it "calls `property_to_s`" 

    end

    context "value is false" do
      it "calfls `property_to_s`" 

    end

    context "value is nil" do
      it "returns a dash" 

    end

    context "value is empty string" do
      it "returns a dash" 

    end
  end

  describe '#property_to_s' do
    it "doesn't modify strings" 


    it "converts floats to currency" 


    it "converts BigDemicals to currency" 


    it "converts `true` to 'Yes'" 


    it "converts `false` to 'No'" 

  end

  describe '#date_with_tooltip' do
    it "doesn't convert time to relative time when unspecified" 


    it "converts time to relative time when specified" 

  end
end

