# frozen_string_literal: true

require 'spec_helper'

describe "Modules" do
  context "A custom module with a main-apps controller" do
    before { authorize_user(:as_admin) }

    it "should have a button in main_navigation, pointing to the configured controller" 

  end
end

