require "app/services/resolve_config_aliases"

describe ResolveConfigAliases do
  describe "#call" do
    context "when the config contains aliases" do
      it "renames them to the appropriate linter" 

    end
  end
end

