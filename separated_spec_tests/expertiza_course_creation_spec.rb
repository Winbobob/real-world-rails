describe 'add TA', js: true do
  before(:each) do
    @course = create(:course, name: 'TA course')
    ta_role = Role.create(name: "Teaching Assistant")
    ta_role.save
  end

  it "check to see if TA can be added and removed" 


  it "should display newly created course" 

end

