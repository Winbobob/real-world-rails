module RSpec
  module Mocks
    RSpec.describe "PreciseCounts" do
      before(:each) do
        @double = double("test double")
      end

      it "fails when exactly n times method is called less than n times" 


      it "fails fast when exactly n times method is called more than n times" 


      it "fails when exactly n times method is never called" 


      it "passes if exactly n times method is called exactly n times" 


      it "returns the value given by a block when the exactly once method is called" 


      it "passes mutiple calls with different args" 


      it "passes multiple calls with different args and counts" 

    end
  end
end

