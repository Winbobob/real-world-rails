require "spec_helper"

module Refinery
  describe HtmlTruncationHelper, :type => :helper do

    describe "#truncate" do
      it "returns nil if text is not present" 


      context "when preserve_html_tags option is present" do
        it "preserve html tags when truncating text" 

      end

      context "when preserve_html_tags option is not present" do
        it "falls back to original truncate method" 

      end
    end

  end
end

