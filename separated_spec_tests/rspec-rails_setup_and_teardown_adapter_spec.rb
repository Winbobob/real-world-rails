require 'spec_helper'

describe RSpec::Rails::SetupAndTeardownAdapter do
  describe "::setup" do
    it "registers before hooks in the order setup is received" 


    it "registers prepend_before hooks for the Rails' setup methods" 


    it "registers teardown hooks in the order setup is received" 

  end
end

