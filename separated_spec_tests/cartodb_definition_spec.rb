# encoding: utf-8

require 'spec_helper_min'

module Carto
  describe Definition do
    before (:all) { @definition = Carto::Definition.instance }
    after  (:all) { @definition = nil }

    it 'handles inexesitent file paths' 


    it 'doesn\'t read the same file twice' 

  end
end

