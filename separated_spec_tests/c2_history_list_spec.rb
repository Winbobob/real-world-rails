describe HistoryList do
  describe '#events' do
    it "returns proposal creation as the first event" 


    context "when the history contains client data creation events" do
      it "filters client data creation events out" 

    end

    context "when the history contains client data update events" do
      it "does not filter client data update events out" 

    end

    context "when the history contains step creation" do
      it "filters the step creation events out" 

    end
  end
end

