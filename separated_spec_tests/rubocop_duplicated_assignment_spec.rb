# frozen_string_literal: true

RSpec.describe RuboCop::Cop::Gemspec::DuplicatedAssignment do
  subject(:cop) { described_class.new(config) }

  let(:config) { RuboCop::Config.new }

  it 'registers an offense when using `name=` twice' 


  it 'registers an offense when using `version=` twice' 


  it 'registers an offense when using `name=` twice with `cbase`' 


  it 'registers an offense when using `<<` twice' 


  it 'registers an offense when using `spec.add_dependency` twice' 


  it 'registers an offense when `name=` method call is not block value' 

end

