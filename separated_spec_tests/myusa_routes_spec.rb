require 'spec_helper.rb'

describe 'Routes to API' do
  let(:json) { { format: :json } }

  context 'URLs without a version segment' do
    describe 'route to v1 controllers' do
      it 'with a GET to a controller' 

      it 'with a GET to a controller with an ID' 

      it 'with a POST to a controller' 

    end
  end
end

