require 'open_food_network/reports/rule'

module OpenFoodNetwork::Reports
  describe Rule do
    let(:rule) { Rule.new }
    let(:proc) { Proc.new {} }

    it "can define a group proc and return it in a hash" 


    it "can define a sort proc and return it in a hash" 


    it "can define a nested rule" 


    it "can define a summary row and return it in a hash" 

  end
end

