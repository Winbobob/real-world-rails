require "rails_helper"

RSpec.describe Bufferizer do
  let(:user) { create(:user) }
  let(:article) { create(:article, user_id: user.id) }

  it "sends to buffer twitter" 


  it "sends to buffer facebook" 

end

