# frozen_string_literal: true

RSpec.describe "bundle install" do
  context "git sources" do
    it "displays the revision hash of the gem repository", :bundler => "< 2" 


    it "displays the ref of the gem repository when using branch~num as a ref", :bundler => "< 2" 


    it "should allows git repos that are missing but not being installed" 

  end
end

