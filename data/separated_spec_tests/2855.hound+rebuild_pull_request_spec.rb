require "rails_helper"

describe RebuildPullRequest do
  describe "#call" do
    context "when a latest build exists" do
      it "schedules a small build job" 

    end

    context "when a latest build does not exist" do
      it "does not schedule a job" 

    end
  end
end

