require 'spec_helper'

module OptionalSpec
  describe "An optional terminal symbol" do
    testing_expression '"foo"?'
  
    it "parses input matching the terminal" 

  
    it "parses epsilon, recording a failure" 

  
    it "parses input not matching the terminal, returning an epsilon result and recording a failure" 

  end
end

