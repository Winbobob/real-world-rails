# frozen_string_literal: true

require "rails_helper"

RSpec.describe MarkdownHelper do
  describe "#md" do
    it "turns the custom youtube markup into an embed video" 


    it "with table of contents, generates links to every header" 

  end
end

