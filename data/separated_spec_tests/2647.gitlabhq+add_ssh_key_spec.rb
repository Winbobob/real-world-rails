# frozen_string_literal: true

module QA
  context 'Create' do
    describe 'SSH keys support' do
      let(:key_title) { "key for ssh tests #{Time.now.to_f}" }

      it 'user adds and then removes an SSH key' 

    end
  end
end

