# frozen_string_literal: true

require "rails_helper"

RSpec.describe "regulations" do
  context 'redirects missing trailing slash' do
    context "without parameters" do
      it 'to trailing slash' 

    end

    context "with parameters" do
      it 'to trailing slash' 

    end
  end
end

