describe StepDecorator do
  describe "#action_status" do
    it "returns the correct status text" 

  end

  describe "#label" do
    context "when the step is an approval" do
      it "fetches the correct role text" 

    end

    context "when the step is a purchase" do
      it "fetches the correct role text" 

    end
  end

  describe "#role_name" do
    context "when the step is an approval" do
      it "fetches the correct role text" 

    end

    context "when the step is a purchase" do
      it "fetches the correct role text" 

    end
  end

  describe "#action_name" do
    context "when the step is an approval" do
      it "fetches the correct action text" 

    end

    context "when the step is a purchase" do
      it "fetches the correct action text" 

    end
  end

  describe "#waiting_text" do
    context "when the step is an approval" do
      it "fetches the correct waiting text" 

    end

    context "when the step is a purchase" do
      it "fetches the correct waiting text" 

    end
  end

  describe "#completed" do
    context "when the step is an approval" do
      it "fetches the correct completed text" 

    end

    context "when the step is a purchase" do
      it "fetches the correct completed text" 

    end
  end
end

