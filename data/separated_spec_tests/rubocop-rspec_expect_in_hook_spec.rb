# frozen_string_literal: true

RSpec.describe RuboCop::Cop::RSpec::ExpectInHook do
  subject(:cop) { described_class.new }

  it 'adds an offense for `expect` in `before` hook' 


  it 'adds an offense for `expect` in `after` hook' 


  it 'adds an offense for `expect` in `around` hook' 


  it 'adds an offense for `expect` with block in `before` hook' 


  it 'accepts an empty `before` hook' 


  it 'accepts `allow` in `before` hook' 


  it 'accepts `expect` in `it`' 

end

