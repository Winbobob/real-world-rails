describe ScheduledReportsController do
  let(:user) { create(:user, client_slug: "test") }

  before do
    login_as(user)
  end

  describe "#create" do
    it "creates new subscription with JSON" 


    it "creates new subscription with HTML" 

  end

  describe "#update" do
    it "update existing subscription JSON" 


    it "update existing subscription HTML" 

  end
end

