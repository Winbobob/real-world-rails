require 'spec_helper'

describe ChefSpec::ServerRunner do
  context 'when the RSpec config is set' do
    it 'does not create a tmpdir' 

  end

  context 'when the RSpec config is not set' do
    it 'creates and returns a tmpdir' 


    it 'uses the same path' 

  end
end

