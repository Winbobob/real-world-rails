# frozen_string_literal: true

describe QA::Factory::Repository::Push do
  describe '.files=' do
    let(:files) do
      [
        {
          name: 'file.txt',
          content: 'foo'
        }
      ]
    end

    it 'raises an error if files is not an array' 


    it 'raises an error if files is an empty array' 


    it 'does not raise if files is an array' 

  end
end

